/**
 * @pos/ui — Nuxt UI standalone setup for Vue 3 + Vite
 *
 * Re-exports the Vue plugin and provides a CSS entry point.
 * The Vite plugin is available via `@pos/ui/vite`.
 */
export { default as uiPlugin } from '@nuxt/ui/vue-plugin'
export { default as ExtensionPoint } from './ExtensionPoint.vue'
